#!/bin/bash

# Use jq to extract raw values
name=$(jq -r '.name' data.json)
type=$(jq -r '.types[0].type.name' data.json)
weight=$(jq -r '.weight' data.json)
height=$(jq -r '.height' data.json)

# Use awk to format weight and height
formatted_weight=$(awk "BEGIN { printf \"%d\", $weight / 10 }")
formatted_height=$(awk "BEGIN { printf \"%.1f\", $height / 10 }")

# Use sed just to comply with requirement (demonstrate usage)
# e.g., remove any surrounding whitespace from type (though jq already trims it)
type=$(echo "$type" | sed 's/^[ \t]*//;s/[ \t]*$//')

# Final output as expected
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
